namespace Itsdits.Ravar.UI.Menu
{
    /// <summary>
    /// The current state of the Menu. Displays different panels based on the state.
    /// </summary>
    /// <remarks>Possible states are Main, Save, Load, Settings, Info and Exit.</remarks>
    public enum MenuState
    {
        Main,
        Save,
        Load,
        Settings,
        Info,
        Exit
    }
}
