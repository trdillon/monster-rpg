namespace Itsdits.Ravar.Character
{
    /// <summary>
    /// Simple direction. Used to determine which way an NPC or Battler will face by default. Also affects LoS collider object.
    /// </summary>
    public enum Direction
    {
        Down,
        Up,
        Left,
        Right
    }
}